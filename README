Autotools project README file

See http://nRF24.github.io/RF24

pre-requisites
sudo apt install autotools-dev autoconf libtool



./autogen.sh
./configure --sysconfdir=/etc --with-rf24
make
sudo make install
sudo update-rc.d rf24pi defaults

t="$(mktemp -d)"
DESTDIR="$t" make install